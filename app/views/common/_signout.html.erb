<% if signed_in? %>
  <%= form_for :session, url: signout_path, method: 'delete', class: 'navbar-form' do |f| %>
    <%= f.submit "Sign out", id: 'signout', class: 'btn-sm' %>
  <% end %>
<% else %>
  <%= link_to "Sign In", signin_path %>
  <%= link_to "Sign up", signup_path %>
<% end %>
